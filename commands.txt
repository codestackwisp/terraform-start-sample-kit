# Initialize

terraform init

# Check formatting

terraform fmt -check -recursive


# Validate Terraform configuration

terraform validate

# Run a plan (test what will happen)

terraform plan -out=plan.tfplan